# 证券交易模拟器设计方案与真实市场复现可行性调研报告

> 版本:v1.0(2026-08-15)
> 范围:模拟器总体架构设计(交易所 / 市场仿真 / 前端可视化)+ "校准参数集复现真实标的并预测 T+1"的文献调研与可行性结论

---

# 第一部分 证券交易模拟器设计方案

## 0. 三条核心设计原则

1. **K线不是画出来的,是撮合出来的**。所有价格必须由订单簿真实成交产生(订单流 → 撮合 → 成交打印 → 聚合成K线),任何模块都不允许直接"写价格"。这是形态无法分辨的根本前提。
2. **微观真实性靠 Agent 行为,宏观形态靠情景调制,两层分离**。Agent 群体自然涌现厚尾、波动聚集等特征事实;宏观引擎只调参数(波动率乘子、羊群系数、新闻到达率),绝不干预价格本身——类似 L4D 的 AI Director。
3. **性能思路抄游戏:不轮询、批量、分层聚合**。"数万投资者 × 毫秒级"是个陷阱——真实市场里 99% 的投资者在任何一毫秒内都是沉默的。做的是**事件驱动唤醒**,不是逐 tick 轮询全体。

## 1. 总体架构

```
┌─────────────────────── sim-server (Rust 单进程多线程) ───────────────────────┐
│                                                                              │
│  情景引擎 ──每tick输出参数向量──► Agent 工作线程池 (N 线程, SoA 数组)         │
│  (Markov制度切换)                 ▲ 泊松唤醒队列 (min-heap)                   │
│                                  │                                           │
│  玩家订单 (网关) ──────────────►  │                                           │
│                                  ▼                                           │
│                     撮合引擎 (按标的分片, 每线程单线程撮合)                    │
│                                  │                                           │
│              ┌───────────────────┼──────────────────┐                        │
│              ▼                   ▼                  ▼                        │
│        K线聚合器            L2盘口/逐笔流        事件溯源日志                  │
│        (1s基础bar)          (增量+序号)          (replay/加速生成)           │
│              └───────────────────┼──────────────────┘                        │
│                                  ▼                                           │
│                    gateway (tokio, WebSocket 二进制推送)                      │
└──────────────────────────────────┬───────────────────────────────────────────┘
                                   ▼
                 前端 (React + Canvas: K线/盘口/下单/账户)
```

**关键解耦:虚拟时钟 ≠ 墙钟。** 同一套确定性引擎(固定随机种子、事件溯源)可离线以最高速度快进——几分钟内生成 3 年 1 分钟K线作为玩家入场前的"历史行情",玩家入场后切换 1 倍速实时仿真。历史与实时无缝衔接。

## 2. 证券交易所模块

订单簿 + 撮合,对标真实交易所微结构。

- **订单簿**:价格-时间优先。价格阶梯用 `BTreeMap<Price, VecDeque<Order>>`,撤单用 `HashMap<OrderId, 位置句柄>` O(1) 定位。
- **订单类型**:限价、市价、撤单/改单;可选冰山单(大单拆分用,对冲击成本真实性很重要)。
- **撮合线程模型**:每个标的组一个线程,线程内单线程无锁(真实交易所即如此,LMAX 模式)。线程间用 crossbeam 无锁 SPSC 队列传订单。Rust 单核撮合吞吐 ≥ 50 万订单/秒,远超需求。
- **A股规则(以A股为目标,全部可配置)**:集合竞价(9:15–9:25 集中撮合)、连续竞价、午休、T+1、±10%/±20% 涨跌停(涨跌停时订单堆积在板上的排队形态,恰恰是真实感的来源)、最小变动价位 0.01。
- **行情输出**:L2 十档增量 + 快照、逐笔成交/逐笔委托流,带序号,断线 resync——协议直接照抄真实行情网关,前端天然像真的。

## 3. 市场仿真模块(核心)

### 3.1 Agent 分类学(数量是配比,总量 5 万+)

| 类型 | 占比 | 决策频率 | 规则要点 | 负责什么 |
|---|---|---|---|---|
| 做市商 | ~10/标的 | 10–100 Hz | 库存管理 + 动态价差 | 价差、盘口深度、即时流动性 |
| 高频动量 | 0.5% | 1–10 Hz | 短线突破/订单流失衡 | 短期价格发现 |
| 趋势交易 | 5% | 0.1–1 Hz | 均线/突破跟随 | 中期趋势段 |
| 均值回归 | 10% | ~0.1 Hz | 偏离锚定价反向 | 震荡、遏制单边 |
| 基本面 | 20% | 日~周 | 对"真实价值"(隐藏随机游走)折现 | 长期锚 |
| 噪声交易 | 60% | 泊松 | 零智力:中间价 ± 厚尾随机挂单 | 成交量基底、流动性 |
| 事件驱动 | 5% | 事件触发 | 新闻方向+衰减反应 | 跳空、脉冲放量 |
| 庄家(可选) | 极少 | 计划性 | 拆单、建仓-拉升-出货 | 游戏性:操纵形态 |

### 3.2 调度:"数万 × 毫秒级"的正确解法

**每个 Agent 一个泊松时钟 + 全局最小堆唤醒队列:**

```
优先队列元素: (wake_time, agent_id)
pop → 读 SoA 数组状态 → 行为规则 → 产出意图(挂/撤/改单)
    → 送撮合队列 → 按新的泊松间隔(受活跃度乘子调制) push 回队列
```

吞吐账:10 万 Agent,平均唤醒率 0.05 次/秒 → **稳态约 5000 次决策/秒,每毫秒仅个位数 Agent 活跃**。做市商贡献高频报价流(每标的几百事件/秒)。总订单流 < 5 万/秒,单撮合线程绑绑有余余,多标的分片后毫无压力。

### 3.3 游戏引擎 DIY 性能技巧 → 本项目的映射

| 城市天际线 / 戴森球计划的做法 | 本方案对应 |
|---|---|
| 市民 Agent 只在需要时激活(选择性仿真) | 泊松唤醒队列,永不逐 tick 轮询全体 |
| LOD:远离镜头的粗粒度仿真 | 长周期 Agent(基本面)降频到日级;只有"贴近盘口"的高频类型享受毫秒级精度 |
| ECS / 面向数据设计(SoA、批处理、cache 友好) | Agent 状态存 SoA 平铺数组(`intent[]`, `position[]`, `rate[]`…),按类型分桶,系统批量处理,无虚函数分发、无 GC、零运行期分配(预分配固定容量池) |
| 分帧预算:每个系统给固定时间片,超时降载 | 每个系统有 µs 级预算(见 §6),决策超时就推迟到下一 tick,行情永远不会卡 |
| 分层聚合:传送带→工厂→星球,上层只看聚合 | tick→订单流→bar→指标,K线聚合器只消费成交打印;前端只消费聚合结果 |
| 时间加速/快进(睡觉跳过) | 虚拟时钟与墙钟解耦,离线全速生成历史K线 |

### 3.4 形态真实性的来源(特征事实对照表)

"无法分辨"的验收清单,每一条都有明确的生成机制:

| 真实市场特征事实 | 生成机制 |
|---|---|
| 收益率尖峰厚尾(峰度 3~30+) | 羊群系数(Cont-Bouchaud 聚类)+ 制度切换 + 新闻跳跃 |
| 波动率聚集(GARCH 效应) | Agent 时间尺度异质性 + 羊群正反馈:跌→悲观者活跃→更跌 |
| 收益率无自相关(弱式有效) | 多空对称 + 做市商吃掉可预测偏离 |
| \|r\| 长记忆(Hurst≈0.5–0.6) | 长短周期 Agent 混合 |
| 量-波动正相关 | 活跃度乘子受情景引擎统一调制 |
| 价格冲击平方根律 | 大单 Agent 按参与率拆单 + 冰山单 |
| 日内 U 型成交量 | 时段活跃度乘子(开盘/收盘高,午间低) |
| 涨跌停排队、跳空 | 交易所规则本身 + 隔夜信息重估 |

宏观情景引擎:隐 Markov 链在 {牛市/熊市/震荡/危机} 间切换(转移矩阵按真实市场牛熊持续期校准),每 tick 输出参数向量 `(漂移, 波动率乘子, 羊群系数, 新闻到达率, 活跃度乘子)`。**它只改 Agent 参数,不碰订单簿。**

## 4. 前端可视化模块

- **技术**:React + Canvas 自绘(参考 klinecharts / TradingView lightweight-charts 的增量绘制方式),WebSocket 二进制(FlatBuffers/msgpack)。
- **协议**:入场 `hello → 全量快照(K线/盘口/账户) → 增量流(带 seq,检测到缺口自动 resync)`——和真实行情终端一模一样。
- **功能对齐证券 App**:分时/1s/1m/5m/日K(可缩放拖拽)、L2 十档盘口、逐笔成交、买卖队列、限价/市价下单、撤单改单、资产/持仓/委托/成交流水、键盘快捷下单。
- **渲染性能**:只绘制可视区间的 bar;实时更新只重绘最后一根 + 脏区;requestAnimationFrame 节流合并推送;历史长区间用预聚合的粗粒度 bar(LOD 思想再次复用)。
- **公平性**:玩家订单进同一个撮合引擎,同样排队、同样滑点、同样吃冲击成本。

## 5. 校准与验收("无法分辨"怎么证明)

1. **统计回归测试套件(CI 化)**:批量生成 1000 标的 × 3 年 1 分钟K线,自动检验:收益率峰度>3 且 |r| 的 ACF 显著衰减缓慢、r 的 ACF 不显著、Hurst ∈ [0.5, 0.6]、量-波动回归显著为正、价差/冲击分布拟合。违反任一即构建失败。
2. **参数校准**:用矩匹配(或近似贝叶斯计算 ABC)把 Agent 参数拟合到真实A股/美股历史数据。
3. **盲测图灵测试**:真实与仿真的裸K线(无标签)混排给人类辨别,正确率应接近 50%。

## 6. 技术选型与 1ms tick 预算

| 层 | 选型 | 理由 |
|---|---|---|
| 仿真核心+撮合 | **Rust**,单进程 | 无 GC 停顿(毫秒级的命门)、SoA/cache 友好、确定性可 replay |
| 并发 | crossbeam 无锁队列 + 按标的分片撮合线程 | 真·无锁热路径 |
| 网关 | tokio 异步 WebSocket | IO 密集,与热路径隔离 |
| 前端 | React + Canvas + WebSocket | 生态成熟、增量绘制性能足够 |
| 存储 | 事件溯源日志 + Parquet 历史K线 | replay、审计、加速生成 |

**单 tick(1ms)时间预算:** 唤醒+决策 300µs | 撮合 300µs | K线聚合 100µs | 行情发布 100µs | 情景引擎 50µs | 余量 150µs。任一系统超预算即降载(决策推迟),保证 tick 永不超时——这就是游戏引擎"分帧预算"的纪律。

## 7. 落地路线建议

1. **M1**:撮合引擎 + 噪声 Agent + K线聚合 → 验证零智力模型已能产出真实价差/成交量分布(Farmer 定理)。
2. **M2**:全类型 Agent + 情景引擎 → 跑统计验收套件,迭代校准参数。
3. **M3**:虚拟时钟加速生成历史 + 前端完整 UI + 玩家交易闭环。
4. **M4**:A股微结构细则(集合竞价/涨跌停排队/T+1)、庄家 Agent、盲测调优。

最大风险不在性能(上述设计吞吐富余一个数量级),而在**校准**——M2 的验收驱动迭代应占整个项目一半以上的工作量。

---

# 第二部分 真实市场复现与 T+1 预测可行性调研

## 摘要(一句话结论)

**"校准参数集复现某个真实标的历史走势"——学界已做到(含A股相关指数);"由此外推出 T+1 那条确定的K线"——被理论和实证双重否定,存在硬上限;"预测 T+1 的分布(概率锥)"——可行,且正是当前研究前沿。** 该想法把"拟合"和"预测"合在一起,拆开后一半是已解决的,一半是有根本性障碍的。

## 1. 问题拆解:四个层次

| 目标 | 可行性 | 状态 |
|---|---|---|
| A. 找到参数集,统计上复现某真实标的的历史(特征事实、形态) | ✅ 成熟 | SMM/ABC/深度代理校准,已做到个股与指数级 |
| B. 逐 bar 复现历史轨迹本身 | ⚠️ 能做到但无意义 | 本质是插值/重放,参数越多越过拟合 |
| C. 预测 T+1 那条**确定的**K线 | ❌ 有硬上限 | 日级方向准确率实证上限 ~52–58%,R² < 1% |
| D. 预测 T+1 的**分布**(概率锥、情形树) | ✅ 前沿可行 | 市场生成器、集合预报思想 |

"通过已知数据仿真系统下一刻状态"在文献里的标准名称是**市场模型的逆向问题(inverse problem)/校准(calibration)+ 条件生成预测**。

## 2. 五条研究线

### 线 1:ABM 校准到真实市场(该想法的直接对应物)

- **Recchioni, Tedeschi & Gallegati (2015, JEDC)**:把 Brock-Hommes 异质交易者模型(基本面+趋势两类 agent)用约束非线性优化校准,**成功复现了 S&P 500、Euro Stoxx 50、Nikkei 225 和沪深300(CSI 300)的价格序列**,并尝试用校准参数做价格预测——扩充交易者信息集后预测有改进。
- **Lamperti et al. (2017/2018)**:用机器学习**代理模型(surrogate)** 替代昂贵的 ABM 评估来做校准,把计算成本降低几个数量级,已应用于 Brock-Hommes 模型。
- **Zegadlo (ICCS 2021)**:指出关键坑——SMM 的损失函数是**带噪声、多峰的**,即使找到全局最优也未必是真参数。
- **ANTR (2026, arXiv:2601.06920)**:用可预训练的**神经密度估计器直接建参数后验分布** P(参数|观测数据),支持跨市场批量校准。方向性变化:从"找一个最优参数集"进化到"求参数的后验分布"。

**关键结论:校准能做到统计意义上的复现,但参数不可识别(等终性,equifinality)——许多不同参数集产生统计上不可区分的路径。**这直接限制 C。

### 线 2:"系统下一刻状态"预测最成功的案例——但只在微观尺度

- **Sirignano & Cont (2019, Quantitative Finance)**:用数十亿条美股订单簿数据训深度网络预测下一价格变动方向。发现价格形成机制是**普适(跨股票,连训练集外的股票都适用)、平稳(跨年份)、路径依赖**的。短视界(下一个订单簿事件,毫秒~秒级)方向准确率约 60%+。
- **Lucchese et al. (2022)**:系统研究后结论是"高频收益的可预测性**无处不在**",但强依赖订单簿表示方式,且随视界拉长迅速衰减。
- **DeepLOB (Zhang et al. 2019)**、Barbon 的注意力模型:同一方向的后续工作。

**启发:把市场当系统预测"下一状态"确实成立——但成立的尺度是订单流级别,不是日K级别。** 预测能力随视界指数衰减,到日级只剩 52–58% 方向准确率(线 4)。

### 线 3:数据驱动市场模拟器(Market Generators)——绕开手写 Agent

- **Buehler et al. (2020)**:小数据环境下的市场生成器,路径签名(signature)+ VAE,条件生成"下一时段市场状态",服务于 Deep Hedging。
- **J.P. Morgan AI Research 系列(Coletta et al.)**:LOB-GAN → **"世界智能体(world agent)"**(2022,直接从历史数据学一个整体市场响应者,取代手工校准的多 agent 池)→ **ATMS (2023)**,用 RL 训模拟器 → **TRADES (2025)**,Transformer 扩散模型生成订单流,配套开源框架 DeepMarket。
- **因果市场模拟器 (2025)**:VAE + 结构因果模型,生成反事实市场轨迹。

这条线证明:**"给定当前状态,生成下一刻的市场分布"是可实现的工程目标**——但它输出的是分布/采样,不是确定性预测。

### 线 4:日级预测的实证上限

- **Gu, Kelly & Xiu (2020, RFS)**:最权威的大规模实证。最好的神经网络月度收益 OOS R² 仅 ~0.4%。原文点破本质:"**市场效率迫使收益变动被不可预测的新闻所主导**"。但 0.4% 的 R² 已足以把择时夏普从 0.51 提到 0.77——微弱的可预测性有经济价值。
- 开源复现与文献综合:LSTM 日线方向预测 ~55%(随机基准 50%);蜡烛图 CNN/LSTM 最好 ~63.9%(还是"当日最后一小时"这种短视界);A股因散户占比高,文献报告 55–65%,但波动大、难稳定。

### 线 5:混沌与敏感性

- 多项研究(Grassberger-Procaccia 相关维数、最大 Lyapunov 指数)给出金融市场**正 Lyapunov 指数**的证据(如 λ≈0.44),即对初始条件指数敏感——确定性混沌特征。另有研究发现混沌强度在危机前系统性上升,可做预警指标。
- **含义:即使仿真器与真实市场方程完全一致,初始状态的测量误差也会让确定性预测误差随时间指数放大。**这与天气预报的处境相同——气象学的应对不是放弃,而是改用集合预报。

## 3. 为什么"参数足够细"推不出"确定预测 T+1"

四个独立起作用的障碍,每个都足以封顶:

1. **不可识别性**:不同参数集 → 统计不可区分的路径(线 1)。校准不唯一,则外推不唯一。加参数只会加剧(等终性是 ABM 的固有属性)。
2. **混沌敏感性**:正 Lyapunov 指数意味着预测视界有限,且市场维度远高于 Lorenz 系统。
3. **反身性与对手适应**:这是与天气预报的本质区别——大气不会因为预报而改变行为,市场会。任何被发现的稳定预测信号会被套利交易迅速削弱(Lucas 批判的金融版)。Sirignano-Cont 的"普适平稳"之所以成立,恰因为它捕捉的是微观结构冗余(订单流动力学),而不是可被套利的日级信号。
4. **外生信息**:T+1 的K线由隔夜公告、政策、突发新闻驱动,这些信息物理上不在历史K线中。模型最多能预测"无新闻条件下的分布",而新闻是分布的混合权重。

第 4 条也解释了为什么"参数量→∞"这条路走不通:参数再多也无法编码尚未发生的信息。**信噪比是天花板:日波动 ~2%,可预测成分 <0.5%,这意味着分布的中心和形状可预测,实现值不可预测。**

## 4. 可行版本:市场数字孪生 + 集合概率预报

气象学在同样困境下(混沌、1970 年代放弃单值预报)发展出的方法论,可以直接平移:

```
历史数据(逐笔/日K/资金流)
   │  1. 离线校准:ABM参数的后验分布 P(θ|data)   ← 线1+线3的技术
   ▼
参数后验 θ₁...θ_K(不是单点,是分布)
   │  2. 数据同化:每天收盘后用滤波器(EnKF/particle)更新 θ
   ▼
3. 集合预报:从后验采样 K 组参数 × M 个随机种子
   │  跑 K×M 条 T+1 路径
   ▼
4. 输出概率锥:明日收阳概率 58%、区间 [a,b] 覆盖率 80%、
   涨停概率 3%、量能分布……  ← 验收指标是校准度(calibration)、
                                CRPS、覆盖率,不是命中率
```

这个版本在科学上站得住(它不宣称能预测实现值),且每一环都有现成文献支撑:校准后验(ANTR 2026)、条件生成 T+1(TRADES 2025、Buehler VAE 2020)、数据同化(EnKF 是气象标准工具,已有用于金融的研究)。

**验收的正确姿势**:不是"预测的K线=真实K线"(伪目标),而是——

- 真实 T+1 落在预测分布 80% 区间内的频率 ≈ 80%(校准度);
- 预测分布比无信息基准(如历史 bootstrap)CRPS 更低(技巧分)。

## 5. 对本项目的具体建议

1. **把"复刻真实标的"做成玩法**:校准管道(SMM + 神经代理)离线跑,给每个 AI 标的配一个后验参数分布而非单点参数——这顺便解决了第一部分方案里参数拍脑袋的问题。
2. **概率锥作为高级功能**:玩家付费/升级解锁"分析师预测"——显示的不是一条K线而是分布扇形。真实、诚实、且市场上没有游戏这么做。
3. **不要做"预测明天K线"的核心玩法**:若玩家发现 AI 能精准预知明日走势,等于宣告整个撮合仿真失效。
4. A股数据适合这个方向:散户占比高 → 特征事实更鲜明 → ABM 拟合效果好(Recchioni 拿 CSI 300 验证过);逐笔数据(Level-2)是校准的最好燃料。

## 6. 核心文献清单

| 主题 | 文献 |
|---|---|
| ABM 校准复现真实指数(含 CSI300)并预测 | Recchioni, Tedeschi & Gallegati 2015, JEDC |
| ML 代理加速 ABM 校准 | Lamperti, Roventini & Sani 2017 |
| SMM 损失函数噪声/多峰问题 | Zegadlo, ICCS 2021 |
| 神经后验估计校准(2026) | ANTR, arXiv:2601.06920 |
| 微观结构普适可预测性 | Sirignano & Cont 2019; Lucchese et al. 2022 |
| 市场生成器 | Buehler et al. 2020; Coletta et al. 2021–2023; TRADES 2025 |
| 日级预测实证上限 | Gu, Kelly & Xiu 2020, RFS |
| 金融市场混沌证据 | Zenagui 2026; Meza Pariona 2025 |

## 参考链接

- Recchioni et al. — A calibration procedure for analyzing stock price dynamics in an agent-based framework: https://www.researchgate.net/publication/281459795
- Lamperti et al. — Agent-Based Model Calibration using Machine Learning Surrogates: https://arxiv.org/pdf/1703.10639
- Zegadlo — Efficient calibration of a financial ABM using MSM: https://www.iccs-meeting.org/archive/iccs2021/papers/127440306.pdf
- ANTR — Calibrating Agent-Based Financial Markets Simulators (2026): https://arxiv.org/html/2601.06920v1
- Sirignano & Cont — Universal features of price formation in financial markets: https://arxiv.org/pdf/1803.06917.pdf
- Lucchese et al. — The Short-Term Predictability of Returns in Order Book Markets: https://arxiv.org/html/2211.13777
- Buehler et al. — A Data-driven Market Simulator for Small Data Environments: https://arxiv.org/pdf/2006.14498
- Coletta et al. — Towards Realistic Market Simulations: a GANs Approach: https://ar5iv.labs.arxiv.org/html/2110.13287
- Coletta et al. — Learning to simulate realistic LOB markets from data as a World Agent: https://arxiv.org/pdf/2210.09897
- Wei et al. — ATMS: Algorithmic Trading-Guided Market Simulation: https://arxiv.org/pdf/2309.01784v1
- Berti et al. — TRADES: Generating Realistic Market Simulations with Diffusion Models: https://arxiv.org/pdf/2502.07071v3
- Thumm & Ontaneda — Towards Causal Market Simulators: https://arxiv.org/html/2511.04469v3
- Gu, Kelly & Xiu — Empirical Asset Pricing via Machine Learning, RFS 2020: https://academic.oup.com/rfs/article/33/5/2223/5758276
- Zenagui — Chaotic Dynamics in Financial Markets: Early-Warning Signals: https://www.preprints.org/manuscript/202604.1830
- Meza Pariona — Fractals and Lyapunov exponents in financial systems: https://dialnet.unirioja.es/descarga/articulo/10524220.pdf
